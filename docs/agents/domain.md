# Domain docs

## Layout

Single-context: one `CONTEXT.md` and one `docs/adr/` directory at the repo root.

## Consumer rules

- Read `CONTEXT.md` first when entering a new area of the codebase.
- Check `docs/adr/` for architectural decisions related to the current task.
